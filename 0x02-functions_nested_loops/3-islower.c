#include "main.h"

/**
 * _islower - prints all alphabet
 *@c: Char in ASCII code
 (*
 * this program prints all alphabet in lower case 10 times
 * Return: 1 if lower case char else return 0 (Success)
 */

int _islower(int c)
{
	if (ch >= 'A' && ch <= 'Z')
		return (0);
	else
		return (1);
}
